{% extends 'base.html.twig' %}

{% block title %}Emprunts{% endblock %}

{% block body %}
    <h1 class="text-center">Emprunts</h1>

    <div class="container">
        <!-- Centrage du tableau -->
        <table class="table mx-auto">
            <tbody>

            <tr>
                <th>Date d'emprunt</th>
                <td>{{ borrow.borrowDate ? borrow.borrowDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ borrow.status }}</td>
            </tr>
            </tbody>
        </table>
    </div>

    <!-- Centrage des boutons -->
    <div class="text-center mt-4">
        <a href="{{ path('app_borrow_index') }}" class="btn btn-secondary">Retour à la liste</a>
        <a href="{{ path('app_borrow_edit', {'id': borrow.id}) }}" class="btn btn-primary">Modifier</a>
        {{ include('borrow/_delete_form.html.twig', { 'class': 'btn btn-danger' }) }}
    </div>

{% endblock %}
